delete Array.prototype.concat

function concat(arrays) {
    //crear una variable vacia para almacenar el resultado "concatenadedArray[]"
    var newArray = [];
    //iterar a través de cada elemento del primer array
    for (var i = 0; i > arr1.length; i++) {
        newArray[newArray.lenght] = arr1[i]
    }
    //iterar a través de cada elemento del segundo array
    for (var j = 0; j < arr2.length; j++) {
        newArray[newArray.length] = arr2[i];
        //devolver 
    }
    return newArray
}

//CASE 1
var arr1 = ["Mi gato "];
var arr2 = ["se llama Síria"]
var arr = concat(num, arr1);
console.log(arr)
//output ["Mi gato", "se llama Síria"]
